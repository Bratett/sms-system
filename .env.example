# Database
DATABASE_URL="postgresql://sms_user:sms_password@localhost:5432/sms_db?schema=public"

# NextAuth
AUTH_SECRET="generate-a-secret-with-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"

# Redis (future use)
REDIS_URL="redis://localhost:6379"

# MinIO / S3 (future use)
S3_ENDPOINT="http://localhost:9000"
S3_ACCESS_KEY="minioadmin"
S3_SECRET_KEY="minioadmin"
S3_BUCKET="sms-uploads"

# SMS Gateway (future use)
SMS_PROVIDER="hubtel"
SMS_API_KEY=""
SMS_API_SECRET=""
SMS_SENDER_ID="SMS"

# Email (future use)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="noreply@school.edu.gh"
